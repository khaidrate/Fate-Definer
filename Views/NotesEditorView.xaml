<UserControl x:Class="FateDefiner.Views.NotesEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BackgroundBrush}">

    <Grid Margin="28,24,28,24">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280"/>
            <ColumnDefinition Width="16"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- ══ LEFT: Note list + search ══ -->
        <Border Grid.Column="0" Style="{StaticResource CardPanelStyle}">
            <DockPanel>

                <TextBlock DockPanel.Dock="Top" Text="SESSION NOTES"
                           Style="{StaticResource SectionLabelStyle}"/>

                <!-- Search bar -->
                <Grid DockPanel.Dock="Top" Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="8"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0"
                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
                    <Button Grid.Column="2" Content="✕"
                            Command="{Binding ClearSearchCommand}"
                            Padding="8,6" FontSize="11"
                            Style="{StaticResource SecondaryButtonStyle}"/>
                </Grid>

                <!-- Add / Delete -->
                <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,6">
                    <Button Content="+ New Note" Command="{Binding AddNoteCommand}"
                            Padding="10,6" FontSize="12" Margin="0,0,6,0"/>
                    <Button Content="Delete" Command="{Binding DeleteNoteCommand}"
                            Style="{StaticResource DangerButtonStyle}"
                            Padding="10,6" FontSize="12"/>
                </StackPanel>

                <TextBlock DockPanel.Dock="Top" Text="{Binding StatusMessage}"
                           FontSize="10" Foreground="{StaticResource AccentBrush}"
                           Margin="0,0,0,6"/>

                <!-- Note list -->
                <ListBox ItemsSource="{Binding Notes}"
                         SelectedItem="{Binding SelectedNote}"
                         ScrollViewer.VerticalScrollBarVisibility="Auto">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="2,3">
                                <TextBlock Text="{Binding Title}"
                                           FontWeight="SemiBold" FontSize="13"
                                           TextTrimming="CharacterEllipsis"/>
                                <TextBlock FontSize="10"
                                           Foreground="{StaticResource MutedTextBrush}"
                                           Text="{Binding SessionDate, StringFormat={}{0:MMM d, yyyy}}"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>
        </Border>

        <!-- ══ RIGHT: Note Editor ══ -->
        <Border Grid.Column="2" Style="{StaticResource CardPanelStyle}">
            <Grid>
            <!-- Empty state -->
            <TextBlock Text="Select a note, or create a new one."
                       Foreground="{StaticResource MutedTextBrush}"
                       VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SelectedNote}" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <DockPanel DataContext="{Binding SelectedNote}">
                <DockPanel.Style>
                    <Style TargetType="DockPanel">
                        <Setter Property="Visibility" Value="Visible"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding}" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </DockPanel.Style>

                <!-- Title row -->
                <Grid DockPanel.Dock="Top" Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="160"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0"
                             Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}"
                             FontSize="18" FontWeight="Bold"
                             Foreground="{StaticResource AccentBrush}"
                             Background="Transparent" BorderThickness="0,0,0,1"
                             BorderBrush="{StaticResource BorderBrush}"
                             Padding="0,4"/>
                    <TextBox Grid.Column="2"
                             Text="{Binding SessionDate, StringFormat={}{0:yyyy-MM-dd}, UpdateSourceTrigger=PropertyChanged}"
                             Padding="8,6"/>
                    <Button Grid.Column="4"
                            Content="Save"
                            Command="{Binding DataContext.SaveCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                            Padding="16,8"/>
                </Grid>

                <!-- Tags -->
                <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,12">
                    <TextBlock Text="Tags: " Foreground="{StaticResource MutedTextBrush}"
                               FontSize="11" VerticalAlignment="Center"/>
                    <TextBox Text="{Binding TagsString, UpdateSourceTrigger=PropertyChanged}"
                             FontSize="11" Width="300"
                             ToolTip="Comma-separated tags, e.g: combat, the-iron-keep, session-5"/>
                </StackPanel>

                <!-- Content editor -->
                <TextBox DockPanel.Dock="Top"
                         Text="{Binding Content, UpdateSourceTrigger=PropertyChanged}"
                         TextWrapping="Wrap" AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         FontSize="14"
                         FontFamily="Segoe UI"/>
            </DockPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
